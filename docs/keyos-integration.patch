diff --git a/Cargo.toml b/Cargo.toml
index da60d9691..7f5db97f4 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -6,6 +6,9 @@ cargo-features = ["trim-paths"]
 [workspace]
 resolver = "2"
 
+# Vendored POC logic (its own sub-workspace; app path-depends into it).
+exclude = ["logic"]
+
 members = [
     "api/app-manager",
     "api/backup",
@@ -37,6 +40,7 @@ members = [
     "apps/gui-app-regulatory",
     "apps/gui-app-security-keys",
     "apps/gui-app-seed-vault",
+    "apps/gui-app-passwords",
     "apps/gui-app-switcher",
     "apps/gui-app-system-actions",
     "apps/simulator",
diff --git a/os/gui-app-launcher/src/main.rs b/os/gui-app-launcher/src/main.rs
index b94d3f0ab..bc044b2b5 100644
--- a/os/gui-app-launcher/src/main.rs
+++ b/os/gui-app-launcher/src/main.rs
@@ -128,6 +128,10 @@ fn app_main(cx: AppContext, ui: AppWindow) {
             HiddenApp { label: "Reg. Testing".into(), app_id: "0xc677731d8ee7380a38faa7cd97cbd3a5".into() },
             HiddenApp { label: "Playground".into(), app_id: "0x7c9f81f9bcee31425062fb0d8fbf3001".into() },
             HiddenApp { label: "Update".into(), app_id: "0x6b713041faef901f23743263a45dcb83".into() },
+            HiddenApp {
+                label: "Passwords".into(),
+                app_id: "0x50617373776f72647300000000000000".into(),
+            },
         ];
 
         ui.global::<State>().set_hidden_apps(slint::ModelRc::new(VecModel::from(hidden_apps)));
diff --git a/xtask/src/main.rs b/xtask/src/main.rs
index ac2a72b4c..56b527c52 100644
--- a/xtask/src/main.rs
+++ b/xtask/src/main.rs
@@ -119,6 +119,7 @@ const DEV_APPS: &[&str] = &[
     // "gui-app-regulatory",
     "gui-app-system-actions",
     "gui-app-update-test",
+    "gui-app-passwords",
 ];
 
 const DEFAULT_SERVICES_HOSTED: &[&str] = &[
@@ -164,6 +165,7 @@ const DEFAULT_SERVICES_HOSTED: &[&str] = &[
     "gui-app-onboarding",
     // "gui-app-recovery",
     "gui-app-file-picker-test",
+    "gui-app-passwords",
     "gui-app-switcher",
     // "recovery-worker",
     "simulator",
